app-id: io.github.immich_folder_watch
runtime: org.freedesktop.Platform
runtime-version: "23.08"
sdk: org.freedesktop.Sdk
command: immich-folder-watch

finish-args:
  - --share=network

modules:
  - name: immich-folder-watch
    buildsystem: simple
    build-commands:
      - echo "Flatpak build placeholder. Implementation planned."
    sources:
      - type: dir
        path: ../../
