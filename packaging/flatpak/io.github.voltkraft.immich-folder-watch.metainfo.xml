<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>io.github.voltkraft.immich-folder-watch</id>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>AGPL-3.0-only</project_license>

  <name>Immich Folder Watch</name>
  <summary>Upload media and synchronize local folders with Immich</summary>

  <developer id="io.github.voltkraft">
    <name>VoltKraft</name>
  </developer>

  <description>
    <p>
      Immich Folder Watch uploads photos and videos from local folders or
      synchronizes folders and albums with your Immich server in both directions.
      Choose a mode for each folder and control album mapping, file filters,
      transfer order, and logging. It runs as a per-user
      desktop app with X11 and XWayland support, talking to Immich over
      its public HTTP API rather than touching the server's storage directly.
    </p>
    <p>Use cases:</p>
    <ul>
      <li>Automatically upload screenshots into a dedicated Immich album.</li>
      <li>Import existing photos and videos, then keep uploading new files.</li>
      <li>Synchronize a local folder with one Immich album, including downloads.</li>
      <li>Map first-level subfolders to Immich albums and keep media outside albums in the source root.</li>
    </ul>
    <p>Highlights:</p>
    <ul>
      <li>Per-folder modes for new-file uploads, existing and new uploads, or bidirectional synchronization.</li>
      <li>Bidirectional sync propagates supported moves, renames, and deletions. Local deletions trash tracked Immich assets; assets removed from the remote source cause permanent deletion of tracked local files after a successful pull.</li>
      <li>Optional album placement for uploads, single-album sync, or subfolders mapped to albums.</li>
      <li>Per-folder extension and exclusion filters, with optional permanent local cleanup after verified uploads in upload modes.</li>
      <li>Waits until files are fully written before upload; retries transient failures.</li>
      <li>Remote change notifications via Socket.IO with polling fallback, plus connection and transfer status in the desktop interface.</li>
      <li>Configurable upload/download order, batching, retries, logging, and start on login.</li>
      <li>English and German UI with OS auto-detection and live language switching.</li>
    </ul>
  </description>

  <launchable type="desktop-id">io.github.voltkraft.immich-folder-watch.desktop</launchable>

  <categories>
    <category>Graphics</category>
    <category>Photography</category>
  </categories>

  <keywords>
    <keyword>immich</keyword>
    <keyword>photo</keyword>
    <keyword>backup</keyword>
    <keyword>upload</keyword>
    <keyword>sync</keyword>
  </keywords>

  <url type="homepage">https://github.com/VoltKraft/immich-folder-watch</url>
  <url type="bugtracker">https://github.com/VoltKraft/immich-folder-watch/issues</url>
  <url type="vcs-browser">https://github.com/VoltKraft/immich-folder-watch</url>

  <screenshots>
    <screenshot type="default">
      <caption>Settings: Immich server, watch sources, per-folder sync mode, and autostart</caption>
      <image>https://raw.githubusercontent.com/VoltKraft/immich-folder-watch/v2.5.0/packaging/flatpak/screenshots/GUI.png</image>
    </screenshot>
  </screenshots>

  <content_rating type="oars-1.1" />

  <provides>
    <binary>immich-folder-watch</binary>
  </provides>

  <supports>
    <control>pointing</control>
    <control>keyboard</control>
    <control>touch</control>
  </supports>

  <requires>
    <display_length compare="ge">640</display_length>
  </requires>

  <branding>
    <color type="primary" scheme_preference="light">#3B7DDD</color>
    <color type="primary" scheme_preference="dark">#1F4F94</color>
  </branding>

  <releases>
    <release version="2.11.0" date="2026-09-09">
      <description>
        <p>Fixed:</p>
        <ul>
          <li>Skipped native Linux D-Bus tests explicitly on other operating systems so the Windows solution test job remains compatible.</li>
          <li>Stabilized upload-order regression checks by waiting for completed transfers before shutdown, preserving coverage of deliberate cancellation retries.</li>
          <li>Applied download order globally across sync sources, albums and unassigned assets, using original creation time first so newer photos in later albums no longer wait behind older unassigned files.</li>
          <li>Restored original creation dates for downloads: Linux uses the modification date; Windows sets the creation date and preserves the remote modification date. Existing unchanged download mappings are corrected with durable recovery that prevents timestamp-only reuploads.</li>
          <li>Enabled Flatpak tray integration through the app's own D-Bus namespace, with menu actions and recovery when the desktop tray host returns.</li>
          <li>Resolved original folder display paths using sandbox-accessible document metadata and opaque portal IDs while retaining granted access paths for synchronization.</li>
          <li>Removed untouched automatic album suggestions when selecting bidirectional sync and reported missing named albums as actionable sync errors with automatic recovery.</li>
          <li>Packaged the current generated square PNG icon instead of stale or non-square launcher assets.</li>
          <li>Brought Linux configuration apply and initial access checks in line with Windows, including local validation, required Immich permissions, shared YAML writing, and explicit failure states.</li>
          <li>Applied configured startup language on Linux and preserved language preferences when saving on both platforms.</li>
          <li>Waited for desktop portal approval before confirming Linux autostart, requested it once on fresh setup, and preserved the choice when hiding the window. Permission dialogs no longer block the UI.</li>
          <li>Restored Linux tray restart, live localized status tooltips, semantic status colors, and graceful explicit shutdown while retaining Flatpak-specific tray and startup behavior.</li>
        </ul>
        <p>Added:</p>
        <ul>
          <li>Added a portal folder replacement action that preserves source settings and a Flatpak-safe session log viewer for Journald logging.</li>
          <li>Added Linux headless GUI regressions, isolated portal protocol tests, and configuration-apply/autostart/language coverage.</li>
        </ul>
        <p>Changed:</p>
        <ul>
          <li>Replaced the app logo with an open golden folder and a compact five-color flower inspired by Immich. The transparent SVG has no white outlines or outer padding, allowing generated icons to use the full available width.</li>
        </ul>
      </description>
    </release>
  </releases>
</component>
